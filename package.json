{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend para uma aplicação de video chamada",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.9.1",
    "mongose": "^0.0.2-security",
    "socket.io": "^4.6.0"
  }
}
